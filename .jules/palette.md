## 2025-05-15 - Standardizing Core Form Component Accessibility
**Learning:** Core form components (Input, Select) must use React's `useId` for robust label-input linkage and implement `aria-invalid` and `aria-describedby` for error message accessibility.
**Action:** Apply the `useId` pattern and ARIA error attributes to all new or modified form components to ensure consistent accessibility.
