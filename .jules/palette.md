## 2025-06-14 - Standardized Form Component Accessibility
**Learning:** Core form components (Input, Select) should use React's `useId` for robust label-input linkage and implement `aria-invalid` and `aria-describedby` for error message accessibility.
**Action:** Always implement these patterns when creating or updating form components to ensure they are accessible by default.
