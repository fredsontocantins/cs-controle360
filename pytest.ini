[pytest]
# pytest configuration file for test discovery and markers

# Test discovery settings
python_files = test_*.py  # Test files must match this pattern
pythonpath = .

# Markers
markers =
    smoke: mark test as smoke test
    integration: mark test as integration test
    regression: mark test as regression test
